<section class="hero py-5">
  <div class="container">
    <div class="row justify-content-center">
      <div class="col-lg-8 text-center">
        <h1 class="display-4 fw-bold text-primary-custom mb-4">
          Prenez le contrôle de vos finances personnelles
        </h1>
        <p class="lead text-muted mb-4">
          MoneyTrack vous aide à suivre vos dépenses, gérer vos budgets et
          atteindre vos objectifs financiers. Une solution moderne et intuitive
          pour une gestion financière simplifiée.
        </p>
        <div class="d-flex gap-3 justify-content-center flex-wrap">
          <% if (typeof user !== 'undefined' && user) { %>
          <a href="/dashboard" class="btn btn-primary btn-lg"
            >Accéder au tableau de bord</a
          >
          <% } else { %>
          <a href="/register" class="btn btn-primary btn-lg"
            >Commencer gratuitement</a
          >
          <a href="#features" class="btn btn-outline-primary btn-lg"
            >Découvrir les fonctionnalités</a
          >
          <% } %>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- Features Section with Bootstrap -->
<section id="features" class="py-5 bg-white">
  <div class="container">
    <div class="row">
      <div class="col-lg-8 mx-auto text-center mb-5">
        <h2 class="display-5 fw-bold text-primary-custom mb-3">
          Fonctionnalités principales
        </h2>
        <p class="lead text-muted">
          Découvrez tous les outils dont vous avez besoin pour gérer
          efficacement votre budget personnel
        </p>
      </div>
    </div>

    <div class="row g-4">
      <div class="col-md-6 col-lg-4">
        <div class="card feature-card h-100 border-0 shadow-sm">
          <div class="card-body p-4">
            <div class="feature-icon mb-3">💰</div>
            <h3 class="h5 fw-semibold text-primary-custom mb-3">
              Gestion des transactions
            </h3>
            <p class="text-muted mb-0">
              Ajoutez, modifiez et catégorisez facilement toutes vos
              transactions. Suivez vos revenus et dépenses en temps réel.
            </p>
          </div>
        </div>
      </div>

      <div class="col-md-6 col-lg-4">
        <div class="card feature-card h-100 border-0 shadow-sm">
          <div class="card-body p-4">
            <div class="feature-icon mb-3">📊</div>
            <h3 class="h5 fw-semibold text-primary-custom mb-3">
              Budgets intelligents
            </h3>
            <p class="text-muted mb-0">
              Créez des budgets personnalisés par catégorie et recevez des
              alertes avant de dépasser vos limites.
            </p>
          </div>
        </div>
      </div>

      <div class="col-md-6 col-lg-4">
        <div class="card feature-card h-100 border-0 shadow-sm">
          <div class="card-body p-4">
            <div class="feature-icon mb-3">🎯</div>
            <h3 class="h5 fw-semibold text-primary-custom mb-3">
              Objectifs d'épargne
            </h3>
            <p class="text-muted mb-0">
              Définissez vos objectifs financiers et suivez votre progression.
              Atteignez vos rêves étape par étape.
            </p>
          </div>
        </div>
      </div>

      <div class="col-md-6 col-lg-4">
        <div class="card feature-card h-100 border-0 shadow-sm">
          <div class="card-body p-4">
            <div class="feature-icon mb-3">📈</div>
            <h3 class="h5 fw-semibold text-primary-custom mb-3">
              Visualisation des données
            </h3>
            <p class="text-muted mb-0">
              Analysez vos habitudes financières avec des graphiques interactifs
              et des rapports détaillés.
            </p>
          </div>
        </div>
      </div>

      <div class="col-md-6 col-lg-4">
        <div class="card feature-card h-100 border-0 shadow-sm">
          <div class="card-body p-4">
            <div class="feature-icon mb-3">🔒</div>
            <h3 class="h5 fw-semibold text-primary-custom mb-3">
              Sécurité maximale
            </h3>
            <p class="text-muted mb-0">
              Vos données financières sont protégées par un chiffrement de
              niveau bancaire. Votre confidentialité est notre priorité.
            </p>
          </div>
        </div>
      </div>

      <div class="col-md-6 col-lg-4">
        <div class="card feature-card h-100 border-0 shadow-sm">
          <div class="card-body p-4">
            <div class="feature-icon mb-3">📱</div>
            <h3 class="h5 fw-semibold text-primary-custom mb-3">
              Interface moderne
            </h3>
            <p class="text-muted mb-0">
              Profitez d'une expérience utilisateur intuitive et responsive,
              accessible sur tous vos appareils.
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- CTA Section with Bootstrap -->
<section class="py-5 bg-primary text-white">
  <div class="container text-center">
    <div class="row">
      <div class="col-lg-8 mx-auto">
        <h2 class="display-5 fw-bold mb-3">
          Prêt à transformer votre gestion financière ?
        </h2>
        <p class="lead mb-4 opacity-75">
          Rejoignez des milliers d'utilisateurs qui ont déjà pris le contrôle de
          leurs finances
        </p>
        <% if (typeof user === 'undefined' || !user) { %>
        <a href="/register" class="btn btn-light btn-lg">
          Créer mon compte gratuitement
        </a>
        <% } %>
      </div>
    </div>
  </div>
</section>

<script>
  // Animation au scroll
  const observerOptions = {
    threshold: 0.1,
    rootMargin: "0px 0px -50px 0px",
  };

  const observer = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        entry.target.classList.add("fade-in-up");
      }
    });
  }, observerOptions);

  document.querySelectorAll(".feature-card").forEach((card) => {
    observer.observe(card);
  });

  // Smooth scroll pour les liens d'ancrage
  document.querySelectorAll('a[href^="#"]').forEach((anchor) => {
    anchor.addEventListener("click", function (e) {
      e.preventDefault();
      const target = document.querySelector(this.getAttribute("href"));
      if (target) {
        target.scrollIntoView({
          behavior: "smooth",
          block: "start",
        });
      }
    });
  });
</script>
